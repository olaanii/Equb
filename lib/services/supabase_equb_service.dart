// Frontend-only build: Supabase service removed.
// This file is intentionally left as a no-op to avoid build errors
// if tools include all Dart files. If you need a Supabase-backed
// implementation, create it in a separate branch or feature flag and
// add the supabase_flutter dependency in pubspec.yaml.
